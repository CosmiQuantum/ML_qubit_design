# Scripts

1. `environment` contains helper scripts for creating and removing the conda environment.
2. `notebook_maintenance` contains targeted utilities for patching notebook text or plot formatting after manual notebook edits.
