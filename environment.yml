name: cryo-modelling-env
channels:
  - conda-forge
dependencies:
  - python=3.9
  - numpy
  - scipy
  - nodejs
  - ipython
  - jupyterlab
  - jupyterlab-git
  - jsonschema-with-format-nongpl
  - pandas
  - joblib
  - pydot
  - graphviz
  - scikit-learn
  - matplotlib
  - reportlab
  - svglib
  - cairosvg
  - pymupdf
  - tensorflow-cpu==2.11.1
  - tensorflow-datasets==4.8.3
  - webcolors
  - widgetsnbextension==3.6.0
  - pip
  - pip:
      - keras-tuner
    # Validation notebooks also depend on the external design and Ansys stack
    # These packages are not pinned here because they often require external desktop software.
    # Add those separately if you plan to run the validation notebooks.
