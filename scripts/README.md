# Scripts

- `environment/`: helper scripts for creating and removing the conda environment.
- `notebook_maintenance/`: targeted utilities for patching notebook text or plot formatting after manual notebook edits.
